/** @type {import('next').NextConfig} */
const nextConfig = {
  remotePatterns: [
    {
      hostname: "avatars.githubusercontent.com",
    },
    {
      hostname: "avatars.githubusercontent.com",
    },
  ],
};

export default nextConfig;
